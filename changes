- updated UtgardeKeep with some code that makes use of the new NPCSpawnPools
- #npc spawn <name> works now (and teleports you to the spawned NPC) -> will also create an instance if it is an instance NPC
- fixed another bug with #map clear